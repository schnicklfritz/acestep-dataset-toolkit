# .changed_markers
# ------------------------------------------------------------------
# CACHE-INVALIDATION NOTICES — read THIS first before touching code.
#
# RULE (see .clinerules): Everything committed at HEAD is an IMMUTABLE CACHE HIT.
# Only open files / hunks listed under [UNREVIEWED] below.
# When you (the user) commit a change, EMPTY this file — the loop has absorbed it.
#
# STALE LINE PROTECTION: bind notes to function/class names + hunk context,
# not raw line numbers (they go stale).
#
# TWO MODES:
#   [UNREVIEWED]      = daily iterative mode (git diff dirty hunks)
#   [BASELINE SWEEP]  = one-time dead-code audit (from vulture/flake8 flags)
# ------------------------------------------------------------------

[UNREVIEWED]
(daily iterative mode — currently no dirty hunks pending)

[BASELINE SWEEP]
# (not yet run) Historical dead code is invisible to git diff (committed =
# clean = cache hit). To populate this section, run once, then delete:
#   vulture . --exclude .venv,kernels --min-confidence 80
#   flake8 --select=F401 .    # unused imports
# Pipe ONLY the flagged lines here; the agent reviews just those candidates.

[FLAGGED - REVIEW / NOT AUTO-FIXED]
dataset_manager.py  GATEKEEPER credential check REMOVED entirely:
   self._prepare_pipeline_credentials(...) + method deleted (grep = 0 hits).
   Confirm intentional before closing.
core/kaggle_worker.py + kaggle_health.py (BROKEN DUPLICATES):
   health-audit remnants w/ `pip install git+https://github.com` + hardcoded
   /kaggle/input/ace-step-upload-bundle. Candidate dead code (confirm via
   BASELINE SWEEP before removal). dataset_manager.py:3249 still references
   core/kaggle_worker.py via start_remote_consolidated_pipeline.

[RESOLVED / CACHE HIT]
(stem-split fix + control/review plumbing verified; workflow + token docs added)
