# AUTO-GENERATED -- do not edit. Regenerate: scripts/gen_changed_markers.sh
# HEAD: 4d4f6909e23a6d4698f46e3c324bf93af709e6b9
# generated: 2026-09-24T03:26:32-04:00
# dirty: 0 path(s), this file excluded
#
# VALIDATE ME BEFORE SCOPING ANY WORK:
#   - '# HEAD:' must equal `git rev-parse HEAD`
#   - the DIRTY block below must match `git status --porcelain`
#     (ignoring .changed_markers itself -- tracked and rewritten by this
#      script -- and .changed_markers.*, which is gitignored scratch).
#   - git status collapses an untracked DIRECTORY to one line while the
#     diff below expands it to one entry per file. Expected, not drift.
#   - cross-check against .agent_notes.md, which is hand-owned.
# On ANY disagreement: STOP, regenerate, re-read. See .clinerules.
#
# ---- DIRTY (real index; one line per git status --porcelain entry) ----
#   (plus .changed_markers and its scratch file: excluded by design)
#
# ------ SCOPE SUMMARY (read this before the hunks) ------

# ------ FULL HUNKS (3 lines of context) ------
# Anchor edits to function/class names + hunk context, never line numbers.

# ------ RENAME GUARD ------
# If the hunks above contain 'rename from', run before committing:
#   git grep -n "old_name"   # who still points at the old name?
#   git grep -n "new_name"   # did the new name actually land?
# Disagreement means UNKNOWN -> stop and ask. See .clinerules.
# (no renames detected in this diff)
