# .changed_markers
# ------------------------------------------------------------------
# CACHE-INVALIDATION NOTICES — read THIS first before touching code.
#
# RULE (see .clinerules): Everything committed at HEAD is an IMMUTABLE CACHE HIT.
# Only open files / hunks listed under [UNREVIEWED] below.
# When you commit a change, EMPTY this file — the loop has absorbed it.
#
# STALE LINE PROTECTION: bind notes to function/class names + hunk context,
# not raw line numbers (they go stale).
#
# TWO MODES:
#   [UNREVIEWED]      = daily iterative mode (git diff dirty hunks)
#   [BASELINE SWEEP]  = one-time dead-code audit (from vulture/flake8 flags)
# ------------------------------------------------------------------

[UNREVIEWED]
# ---------------------------------------------------------------------------
# FEATURE: dedicated corpus-research MCP server (2026-09-18)
#   WHY: a previous session died at the context limit having DESIGNED this but
#   written none of it — it existed only in the session transcript. Recovered
#   from ~/.cline/data/sessions/ and built. Nothing was ever lost from disk:
#   verified there was no write_to_file call for research_tools /
#   mcp_research_server, and no such file existed anywhere.
#
#   The user's idea it serves: gather historical/amateur/production/journalistic
#   descriptions of an artist, then induct the vocabulary those sources actually
#   use, so MOSS-Music picks from a grounded, bounded attribute set instead of
#   inventing descriptors.
#
# NEW (all untracked):
#   modules/research_tools.py     ALL logic, no Qt, no `mcp` import (testable)
#   modules/mcp_compat.py         load_server_class() — mcp 1.x FastMCP / 2.x MCPServer
#   mcp_research_server.py        thin MCP layer; build_server() + main()
#   scripts/build_lexicon.py      CLI: descriptor doc -> docs/vocabulary.txt
#   tests/test_research_tools.py  31 tests
#   tests/test_mcp_servers.py     14 tests (call tools through the MCP boundary)
#   docs/vocabulary.txt           GENERATED (390 terms) — do not hand-edit
#   docs/section_markers.txt      GENERATED (63 markers) — do not hand-edit
#
# MODIFIED:
#   mcp_server.py     import switched to modules.mcp_compat.load_server_class.
#     REASON: installing `mcp[cli]` pulled mcp 2.2.0, which RENAMED FastMCP ->
#     MCPServer. The old `from mcp.server.fastmcp import FastMCP` now raises
#     ModuleNotFoundError, and because that IS an ImportError it was being
#     swallowed into a misleading "pip install mcp[cli]" message. The shim keeps
#     both majors working and reports one honest error.
#   requirements.txt  added a commented `mcp[cli]>=1.0` optional line (house style).
#   .gitignore        added docs/research/ — third-party article text must not be
#                     committed; the corpus lives outside version control.
#   README.md         MCP section now documents BOTH servers + the file tree.
# ---------------------------------------------------------------------------

[UNREVIEWED — previous]
# (daily iterative mode — no other dirty hunks pending)

[DESIGN RULES FOR THE NEW CODE — do not weaken these]
# 1. NO RESEARCH TOOL RETURNS RAW DOCUMENT TEXT.
#    corpus_stats = counts only. grep_corpus = only lines an explicit regex
#    matched, capped, each line also truncated (MAX_LINE_CHARS). Every
#    list-returning function takes a cap. This is what makes it safe to point an
#    agent at a multi-megabyte corpus.
#    Guarded by: test_no_tool_returns_raw_corpus_text,
#                test_token_discipline_holds_across_the_mcp_boundary.
#
# 2. THE LEXICON PARSER MUST SURVIVE documents that mix vocabulary with EXAMPLE
#    CAPTIONS and lyric snippets (docs/descriptor_reference.md does exactly this).
#    extract_descriptor_terms() harvests ONLY `###` term-list blocks (plus the
#    labelled `Genre: ...` form) and skips blockquotes, fenced code blocks and
#    tables. Naive comma-splitting would ingest half a sentence as a "term" and
#    then feed that garbage to the model. _looks_like_prose() is the tripwire;
#    `scripts/build_lexicon.py --check` exits 1 if anything trips it.

[VERIFIED — real runs, not estimates]
#   - full suite green: 314 passed (269 before this work)
#   - scripts/build_lexicon.py --check -> 390 descriptors, 63 markers, exit 0;
#     no term >45 chars, no sentence leaked; LRC metadata tags ([ti:], [ar:],
#     [offset:]) excluded from markers
#   - real stdio handshake against mcp_research_server.py: initialize OK,
#     tools/list -> 9 tools, tools/call corpus_stats(docs) summarized 68,584
#     chars of corpus in ~400 chars of output
#   - registered as "acestep-research" in
#     ~/.cline/data/settings/cline_mcp_settings.json (backup written alongside;
#     verified by diff that nothing else was removed)
#   - Kaggle tools deliberately NOT in autoApprove: they hit the network and
#     spend GPU quota.

[OUTSTANDING / NEXT]
#   - Kaggle tools need credentials from app Settings (outside the repo) before
#     they will do anything but report "No Kaggle credentials".
#   - The corpus does not exist yet. Point --corpus at a folder of prose ABOUT the
#     artist (e.g. /home/fritz/42/Datasets/research/<band>/). Candidate sources
#     already catalogued in docs/master_track_research.md (isolated-tracks.com
#     channel lists, box-set session notes, Clone Hero chart spreadsheets).
#   - The stale VS Code extension MCP config still exists in the OLD format at
#     ~/.config/Code/User/globalStorage/saoudrizwan.claude-dev/settings/
#     cline_mcp_settings.json and its filesystem server entry is malformed
#     (`npx -y` followed by directory paths and no package name). Repair or delete.

[BASELINE SWEEP]
# RUN 2026-09-15 — first pass complete. Tools: vulture 2.16, flake8 7.3.0
# (installed into .venv for this scan; NOT app runtime deps).
#   .venv/bin/python -m vulture . --exclude .venv,kernels,__pycache__,scripts,.git --min-confidence 80
#   .venv/bin/python -m flake8 --select=F401 . --exclude=.venv,kernels,__pycache__,scripts,.git
#
# RESULT: 3906 net lines of dead code removed; app boots clean (offscreen test).
# OUTSTANDING: 41 unused imports in peripheral files (workers/advanced.py(6),
#   workers/structural.py(5), stem_separator.py(5), workers/health.py(4),
#   workers/caption.py(3), ...). Do NOT blindly strip — several of those modules
#   re-import names lazily inside functions. Verify each before removing.

[RESOLVED / CACHE HIT]
# The detailed per-change notes that used to live here (Sep 15-16: the audio-format
# expansion, the "Before You Add Tracks" warning dialog, the dead-code removal
# list, the MOSS device_map=balanced multi-GPU crash and the kernels_output hang)
# all described state that was ALREADY COMMITTED, i.e. cache hits. They are
# recoverable verbatim from git if ever needed:
#     git show HEAD:.changed_markers
#     git show HEAD~N:.changed_markers
# Keep this file transient: it should only ever describe changes NOT yet
# committed. A stale index is worse than no index — it makes an agent scope its
# work off information that no longer matches reality.

