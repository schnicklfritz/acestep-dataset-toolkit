# .changed_markers
# ------------------------------------------------------------------
# CACHE-INVALIDATION NOTICES — read THIS first before touching code.
#
# RULE (see .clinerules): Everything committed at HEAD is an IMMUTABLE CACHE HIT.
# Only open files / hunks listed under [UNREVIEWED] below.
# When you commit a change, EMPTY this file — the loop has absorbed it.
#
# STALE LINE PROTECTION: bind notes to function/class names + hunk context,
# not raw line numbers (they go stale).
#
# TWO MODES:
#   [UNREVIEWED]      = daily iterative mode (git diff dirty hunks)
#   [BASELINE SWEEP]  = one-time dead-code audit (from vulture/flake8 flags)
# ------------------------------------------------------------------

[UNREVIEWED]
# ---------------------------------------------------------------------------
# FIX: the Structural Tag Creator was handed the WRONG vocabulary (2026-09-18)
#
#   SYMPTOM: modules/tag_creator.py built its prompt from a hardcoded
#   ACE_STEP_VOCABULARY constant (34 generic terms) and told the model
#   "Choose tags ONLY from the vocabulary below". Measured against the
#   project's authoritative doc (docs/descriptor_reference.md) it shared
#   only 14 of 34 terms, and it offered "mumble rap" / "chopper rap" /
#   "trap flow" for 1970s hard rock while omitting "downtuned guitar".
#   The doc itself states the precedence rule: "Where it disagrees ... this
#   file wins". The code was contradicting the project's own spec.
#
#   ROOT CAUSE: docs/descriptor_reference.md was documentation only —
#   referenced by ZERO python files. The vocabulary the model actually saw
#   was a generic leftover.
#
#   FIX: the constant is GONE. The prompt is now built from the generated
#   lexicon, scoped per artist.
#
# CHANGED FILES
#   modules/tag_creator.py    ACE_STEP_VOCABULARY removed; load_vocabulary()
#     + load_markers() read docs/vocabulary.json / .txt / section_markers.txt.
#     tag_creator_messages(sample, artist="") gains the artist arg.
#     Prompt gains the doc's spec rules: max 3 descriptors per bracket, and
#     NEVER put BPM/key/time-signature in the caption (Phase 0 saw MOSS
#     volunteer "136 BPM, G major, I-IV-V-vi").
#   workers/tag_creator.py    TagCreatorWorker(..., artist="") passes it through.
#   dataset_manager.py        _tag_creator_artist(): explicit
#     config["tag_creator_artist"] wins, else the dataset metadata name is used
#     as a loose hint. Called from start_structural_tag_creator (the ONE
#     TagCreatorWorker construction site).
#   config.py                 new "tag_creator_artist" default ("").
#   modules/research_tools.py extract_grouped_terms(), resolve_artist(),
#     terms_for_artist(), load_vocabulary_groups(); _add_terms() now dedupes
#     per target list (needed so a term can live in two artists' facets).
#   docs/vocabulary.json      NEW generated artifact (artist -> facet -> terms).
#     docs/vocabulary.txt is BYTE-IDENTICAL to the previous commit — the flat
#     extractor was not disturbed, only the grouping was added.
#   tests/test_tag_creator.py NEW (19 tests). tests/test_research_tools.py +8.
# ---------------------------------------------------------------------------

[UNREVIEWED — previous]
# (none)

# PARSER RULES THAT MATTER (all now covered by tests)
#   - get out of "Complete examples" BEFORE handling bold labels, or
#     **Caption** / **Section tags** become phantom "artists". A blank line
#     must NOT clear the block heading (the example block spans blanks).
#   - outside a `###` block only a labelled line ("Genre: ...") is a term
#     list, or section 10's prose rules get comma-split into fake terms.
#   - reject pieces containing a backtick ("Locales `en`" leaked once).
#   - only register a facet that actually received a term (no empty groups).
#   - _SECTION_ARTIST_OVERRIDES: section 4 is the COUNTRY vocabulary and maps
#     to Hank Williams Sr., not to "general". Without this, pedal steel/banjo
#     were offered to Black Sabbath. General = sections 1-3 only.

[VERIFIED 2026-09-18]
#   - full suite 341 passed (was 314)
#   - build_lexicon --check: 390 descriptors, 56 markers, 4 artists
#     (Hank 142, Sabbath 78, AC/DC 77, Doors 66); zero prose terms; no empty
#     facets; no rap styles anywhere
#   - live prompt: 'sabbath' -> "ARTIST VOCABULARY — Black Sabbath" with
#     downtuned guitar / palm-muted riffs / thunderous drums (4,788 chars);
#     'hank_sr' -> steel guitar / pedal steel / fiddle (6,357 chars)
#   - app boots offscreen with the real DatasetManager; _tag_creator_artist()
#     returns '' on an empty dataset and the config key is present

[OUTSTANDING]
#   - The corpus does not exist yet. Point the research server at a folder of
#     prose ABOUT an artist (e.g. /home/fritz/42/Datasets/research/<band>/).
#     Candidate sources are catalogued in docs/master_track_research.md.
#   - Phase 1 of the MOSS work (the spec conduit for MOSS's own output shape)
#     is only PARTLY addressed: the tag-creator prompt now carries the shape
#     rules, but MOSS still returns markdown and nothing reformats it yet.
#   - The stale VS Code extension MCP config still exists in the OLD format at
#     ~/.config/Code/User/globalStorage/saoudrizwan.claude-dev/settings/
#     cline_mcp_settings.json and its filesystem server entry is malformed
#     (`npx -y` followed by directory paths and no package name). Repair or delete.
#   - /home/fritz/42/Datasets/sabbath/mp3_throwaway is 9-of-15 unusable: 8
#     zero-byte files plus Iron_Man.mp3 truncated to 43.2s (should be ~355s).
#     Regenerate from the FLACs before any captioning run. Verified 2026-09-18.

[BASELINE SWEEP]
# RUN 2026-09-15 — first pass complete. Tools: vulture 2.16, flake8 7.3.0
# (installed into .venv for this scan; NOT app runtime deps).
#   .venv/bin/python -m vulture . --exclude .venv,kernels,__pycache__,scripts,.git --min-confidence 80
#   .venv/bin/python -m flake8 --select=F401 . --exclude=.venv,kernels,__pycache__,scripts,.git
#
# RESULT: 3906 net lines of dead code removed; app boots clean (offscreen test).
# OUTSTANDING: 41 unused imports in peripheral files (workers/advanced.py(6),
#   workers/structural.py(5), stem_separator.py(5), workers/health.py(4),
#   workers/caption.py(3), ...). Do NOT blindly strip — several of those modules
#   re-import names lazily inside functions. Verify each before removing.

