# Agent notes — HAND-OWNED, never machine-written

`scripts/gen_changed_markers.sh` overwrites `.changed_markers` on every run and
knows nothing about this file, so anything written here survives regeneration.

## Two files, two owners

| File | Owner | Contents |
| --- | --- | --- |
| `.changed_markers` (gitignored) | `scripts/gen_changed_markers.sh` | `# HEAD:` stamp, dirty count, scope summary, then the full 3-line-context diff of what is dirty right now |
| `.agent_notes.md` | you, by hand | facts that outlive a regenerated diff: unresolved causes, unverified claims, machine-local damage, the baseline sweep |

The split exists because the generator used to overwrite this prose. A state
ledger that a script can silently delete is worse than no ledger at all.

Housekeeping: when an item here is fixed, delete it or restate it as RESOLVED
with the commit that fixed it. A fixed item left lying around reads as if it
were still true, which is the exact failure this file exists to prevent.

## [OUTSTANDING]

- The 32 zero-byte .mp3 files (plus the stale `dataset-metadata.json`) are STILL in
  `~/acestep_kaggle_staging`. They can no longer reach an upload and the page reports
  them, so this is housekeeping, not a bug: click 🧹 Clean on 🅰 ACE-Step (Kaggle).
- "KAGGLE NEVER CONNECTS" — ROOT CAUSES FOUND AND FIXED (2026-09-21). Three
  independent ones, none of which could be seen from the GUI:
  1. NO CONNECTIVITY CHECK EXISTED. `kaggle_available()` was dead code (zero
     callers) and `refresh_kaggle_cred_status()` inferred health from two
     non-empty strings. Nothing contacted Kaggle until a run had already staged
     and uploaded audio.
  2. THE SDK HID THE REAL FAILURE. In the installed kaggle 2.2.4,
     `authenticate()` tries the access token, then falls back to the legacy
     username/key pair by PRESENCE ALONE (no network check) and reports success —
     so a `KGAT_` access token handed to `KAGGLE_KEY` "connected" and then failed
     every real call with 401. If even that fallback was unavailable it called
     `exit(1)` -> `SystemExit`, a BaseException that a worker's `except Exception`
     cannot catch, so the QThread died with no error dialog at all.
  3. THE ☁️ MASTER PIPELINE BUTTON COULD NOT WORK and never said why:
     `os.system("kaggle datasets version -p <a .zip file>")` (`-p` wants a FOLDER),
     `os.system("kaggle kernels push -p core/kaggle_worker.py")` (a FILE, with no
     kernel-metadata.json anywhere), return codes never checked, output going to
     the terminal rather than the GUI, and `on_remote_pipeline_success` importing
     `core.manifest_sync` — a module that does not exist.
  FIXED: `modules/kaggle.py:probe_kaggle()` (ONE real authenticated call, reports
  the auth method actually used, returns failures instead of raising),
  `preflight_kaggle()` at the top of all seven Kaggle run paths (ACE-Step, MOSS,
  lyrics, stems, structure, spatial, structural), `workers/kaggle_probe.py`
  (catches SystemExit as well as Exception), a **🔌 Test connection** button on
  the ACE-Step credentials row, `slug_problems()` rejecting malformed and
  placeholder dataset slugs, and `KGAT_` values are no longer exported as
  `KAGGLE_KEY`. DELETED (after grepping every anchor): the master button,
  `workers/kaggle_consolidated.py`, `core/kaggle_worker.py`,
  `compress_dataset_folder`, and BOTH duplicate `launch_remote_kaggle_console`
  definitions. `core/file_system.py:execute_disk_rename` is still used and was
  kept. Pinned by `tests/test_kaggle_probe.py`.
- MACHINE-LOCAL, FIXED: `settings.json` had
  `kaggle_model_dataset: "me/my-weights"` — a placeholder that defeated the
  working default in `config.py` and would have made every caption-kernel push
  reference a dataset that does not exist. Reset to
  `michelmoalem9b/acestep-captioner-model` (the export proven in the run recorded
  below). Note also that `~/.kaggle/kaggle.json` does NOT exist on this machine;
  only `~/.kaggle/access_token` (a `KGAT_` token) — so the access-token path is
  the one that has to work here.
- THE PROBE IS THE FIRST THING TO RUN when a Kaggle complaint arrives: it
  distinguishes "the key is bad" from "the key is stored but was sent as the
  wrong KIND of credential", which the old status row could not.
- THE DATASET-READY WAIT NOW SAYS *WHY* IT TIMED OUT (2026-09-21).
  `wait_dataset_ready()` swallows exceptions on purpose — a transient hiccup must
  not end a 5-minute wait — which made a PERMANENT error look identical to a slow
  dataset. Confirmed against the live API: `dataset_status()` returns **404** for
  a dataset this account does not own, and this account currently owns **ZERO**
  datasets. All seven callers now pass `reason=[...]` and fold the last observed
  status/error into their RuntimeError, so a stall reads as
  `last error: HTTPError: 404 ...` instead of a bare timeout.
  THE FIRST RUN IS THE ONE TO WATCH: the audio dataset it creates is the first
  dataset this account has ever owned, so that status call is the only step of
  the pipeline still unverified. `tests/test_kaggle_dataset_wait.py` pins it.
- ALSO VERIFIED LIVE (2026-09-21): `michelmoalem9b/acestep-captioner-model`
  EXISTS and holds `acestep-captioner-export/config.json` plus
  `model-0000{1..5}-of-00005.safetensors` and the index — i.e. the shape the
  caption kernel auto-detects. Note that `dataset_list(search=...)` does NOT find
  it (0 hits) while `dataset_list_files()` does; do not use search to check
  existence.
- THE STAGING FOLDER HOLDS DUPLICATE FORMATS: `~/acestep_kaggle_staging` has 67
  files for ~33 songs (a `.flac` AND an `.mp3` per track). The folder's CONTENTS
  are the uploaded dataset, so the kernel captions both copies — ~2x runtime, with
  the extras reported as unmatched in the review diff. Not a bug; prune by hand.

- ROOT CAUSE OF THE EMPTY KAGGLE MOUNT (fixed in 566f782): five of eight
  workers uploaded the audio dataset and pushed the kernel IMMEDIATELY, before
  the dataset version was mounted, so `/kaggle/input/<slug>` was empty and the
  kernel captioned nothing ("was empty; found audio elsewhere").
  `modules/kaggle.py:wait_dataset_ready()` documented this exact failure but
  only `workers/kaggle_moss.py` and `workers/kaggle_stems.py` called it. All
  five (caption, kaggle_lyrics, spatial, structural, structure) now wait and
  ABORT on timeout. `tests/test_kaggle_dataset_wait.py` pins it.
- THE APP CACHES MODULES AT IMPORT — RESTART IT AFTER PULLING. This is the
  single most likely reason a run STILL failed after the earlier fixes: the
  live process keeps the OLD code (the same trap that once let `{{ATTN_IMPL}}`
  reach Kaggle). Nothing in this repo takes effect in a running app until it
  is relaunched.
- THE CAPTIONER IS PROVEN ON REAL AUDIO (RESOLVED 2026-09-20). A hand-run Kaggle
  notebook captioned the full AC/DC set (`akronohio/acdcdata`, 34 mp3s) with the
  cached weights dataset `michelmoalem9b/acestep-captioner-model` and the schema
  system prompt, in ~20 minutes on 2×T4. Captions came back schema-shaped
  (front-loaded tags, "garage rock", "hard rock", vocal descriptors) and several
  correctly noticed the spoken-word intros on `Bad_Boy_Boogie` / `Dog_Eat_Dog` /
  `Live_Wire`. That run is the reference: it is the same prompt path the app
  pushes.
- RESOLVED 2026-09-24 (commit 124a29f), verified against the tree rather than
  the GUI: `settings.json` says `caption_backend="ace_step"`,
  `caption_staging_dir` and `caption_output_dir` are set, `caption_batch_review`
  is on, and `kaggle_model_dataset` is the cached
  `michelmoalem9b/acestep-captioner-model`. The UI no longer needs the legacy
  ignored `caption_prompt` cleared, because the 🅰 ACE-Step (Kaggle) page has its
  own **Prompt add-on** field (`caption_prompt_addendum`, appended to the user
  turn).
- THE CAPTION KERNEL IS NOW SELF-REPORTING. It prints `[caption]` lines before
  anything slow: python, GPU count, INTERNET reachability, disk, pip results,
  torch/transformers versions, whether the schema arrived, the resolved
  `AUDIO_FOLDER`, the file count, model source, load time, and per-file
  progress. A silent run should no longer be possible — if the log is truly
  empty, the session is not running at all.
- RESOLVED 2026-09-24 (commit 684155e): THE EMPTY RUN WAS NEVER AN ATTACHMENT
  PROBLEM. The caption kernel hard-coded `/kaggle/input/<slug>` while this account's
  inputs mount under the NESTED layout — the kernel's own log proves it, because the
  weights resolved to
  `/kaggle/input/datasets/michelmoalem9b/acestep-captioner-model/...` in the very
  session where the audio folder `/kaggle/input/ace-audio-d66edb` held 0 files. It
  then wrote a valid-looking `{"results": []}`, so the app reported "no captions
  came back" and this ledger blamed the Kaggle side for days.
  The MOSS log quoted here was never evidence: `was empty; found audio elsewhere` is
  MOSS's FALLBACK WORKING — the opposite lesson. `kernels/stem_separation_kernel.py`
  had solved it, `kernels/moss_caption_kernel.py` had copied it, and the caption
  kernel now does too (`_walk_for_audio` + `/kaggle/input` fallback + a HARD FAILURE
  that dumps the input tree), pinned by
  `tests/test_caption_kernel.py::TestAudioDiscovery`.
- THE FIRST RUN CANNOT BE FAST. The model is ~16 GB: download + shard load
  took ~219 s in the user's MOSS log with no download at all. Expect 5-15 min
  of download on the first run of a session; later runs in the same session
  start in ~1 min.
- `docs/kaggle_caption_cell.{ipynb,py}` WERE REMOVED FROM THE REPO (2026-09-20).
  They were hand-paste copies of the Kaggle cell, duplicated in the tree; the app
  pushes `kernels/caption_kernel.py` itself. `scripts/build_caption_notebook.py`
  stays — it is the only offline validator of the template (it refuses to emit an
  unfilled `{{X}}`, which is valid Python and otherwise dies on Kaggle) — and it
  still writes both files ON DEMAND when a hand-run cell is wanted. Its new
  `--prompt-addendum` flag routes through `caption_spec.build_task_prompt`, so a
  hand-built cell gets the same user turn the app sends.
- THE NOTEBOOK'S AUTO-RESOLVE PICKS THE FIRST `/kaggle/input` DIR CONTAINING
  AUDIO. With more than one dataset attached that may be the wrong one — check
  the printed `AUDIO_FOLDER = ...` line in the run log before trusting it. The
  KERNEL's own fallback (added 2026-09-24) shares this caveat; it only runs when
  the expected folder is empty.
- THE AC/DC FILES DO NOT MATCH ACROSS THE TWO PATHS. The local dataset JSON
  (`acdc/_Backup/latest.json`) references `.flac` — 36 samples, 35 usable, 1
  MISSING file — while the user's Kaggle dataset holds mp3s. To import
  `captions_out.json` back into `acdc.json`, match on the filename STEM, not
  the extension, or nothing will line up.
- NO IMPORTER EXISTS FOR THE MANUAL PATH. The app writes captions into the
  dataset itself, but `captions_out.json` from a hand-run notebook must be
  loaded by hand; there is no script for it yet.
- RESOLVED 2026-09-24: `caption_backend` is now `ace_step` in `settings.json`.
  While it said `"custom"` the run went to the Custom endpoint and never touched
  Kaggle (see the grounding caveat on `CustomOpenAICompatBackend`) -- kept here
  because that mis-route is invisible from the Caption page.
- A PROVIDER'S `max_tokens` CANNOT BE TRUSTED — HANDLED, NOT ASSUMED. The
  reported caption was ~2125 words from an endpoint configured with
  `caption_max_tokens=512` (2000 words is ~2700 tokens), so that route ignored
  the cap. `trim_to_caption()` now bounds every backend return and
  `check_caption()` flags >120 words / >900 chars / >3 flow sentences. STILL
  UNVERIFIED: whether that endpoint also ignores the newly added
  `frequency_penalty` — no live API call has been made against it, so the
  penalty path is untested on real traffic.
- EXISTING CAPTIONS ARE UNDER-TAGGED, AND THAT IS MEASURED, NOT GUESSED.
  `modules/caption_audit.py` + `check_caption()` report `sabbath.json` at
  18-of-19 captions and `Doorsdata/doors1.json` at 19-of-19, almost all for one
  reason: "only 3 front-loaded keyword(s); the schema requires 5-12". They are
  NOT corrupted — the tag count is simply short, because they were produced by
  the schema-less prompt. Re-captioning fixes the shape; nothing needs deleting.
- TWO PATHS STILL CAPTION WITHOUT AUDIO, so they can still invent genre/BPM:
  a) `caption_backend "custom"` with `custom_caption_audio=false` in
     `settings.json` -> `workers/caption_backends.py` sends TEXT ONLY (see its
     docstring);
  b) `workers/caption.py _run_deepseek_orchestration` -> filename only.
  The schema fixes the SHAPE of the output, not its GROUNDING. A provenance
  field (`caption_backend` + `audio_sent` per sample) is STILL NOT RECORDED, so
  which path produced a given caption remains unprovable after the fact.
- VERIFY THE CUSTOM ENDPOINT BEFORE BLAMING THE MODEL. `settings.json` has
  `caption_backend "custom"` -> `custom_caption_url
  "https://generativelanguage.googleapis.com"` with `custom_caption_model
  "gemini-3.7-flash"`. Gemini's OpenAI-compatible base URL is normally
  `.../v1beta/openai/`, and that model id looks non-standard. An ungrounded or
  mis-routed request produces exactly the kind of output that started this
  investigation.
- THE UI STILL DISPLAYS THE IGNORED LEGACY PROMPT. `ui/caption_tab.py` loads
  `caption_prompt` into the "Caption Prompt" editor, and `settings.json` still
  holds the old "Start with A or An" text. The runtime ignores it by design
  (`caption_spec.task_prompt_from_config`), so it is harmless but confusing —
  clearing that field in Settings removes the contradiction from view.
- DECODING PENALTIES ARE PROVEN HARMLESS ON HARDWARE (RESOLVED 2026-09-20).
  `caption_repetition_penalty` (1.15) and `caption_no_repeat_ngram` (6) were in
  the successful 34-track run and no caption looped. 1.0 and 0 remain the
  documented "off" values.
- MOSS IS STILL NOT REFORMATTED BY THE APP. The schema now reaches MOSS as a
  prepended prompt (MOSS has no system role), which partly closes the old
  "MOSS returns markdown and nothing reformats it" item — but
  `modules/moss_import.py` still stores MOSS's raw style text as the caption.
- `/home/fritz/42/Datasets/hank_sr/hank.json` is CORRUPT JSON: "Unterminated
  string starting at line 208 column 17". It cannot be loaded. Not repaired.
- PRE-EXISTING flake8 in touched workers: 11 issues (F401/F841/F811) across
  `workers/caption.py`, `workers/spatial.py`, `workers/structural.py`,
  `workers/kaggle_moss.py`. Verified identical before/after the schema commit.
  The baseline sweep says do NOT strip blindly — several are lazy re-imports.
- THE CORPUS IS GITIGNORED AND MACHINE-LOCAL: 32 sources / 557,783 chars under
  `docs/research/<artist>/`. Regenerable, not backed up:
  `scripts/gather_corpus.py --manifest docs/research/sources.json`.
  Extending it is APPEND-ONLY (filenames are `NN-slug.txt` by list position).
  Two sources 403'd: metalinjection.net and sweetwater.com.
- `unlisted_terms` DOES NOT FIND DESCRIPTORS on that corpus (it ranks by raw
  frequency, so it returns proper nouns and years). Run it PER ARTIST, and use
  `grep_corpus` with an explicit candidate list instead.
- RUNNING MCP SERVER CACHES MODULES AT IMPORT — restart it after editing
  `research_tools.py` or the server before trusting a `corpus_stats` call.
- The stale VS Code extension MCP config still exists in the OLD format at
  `~/.config/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`;
  its filesystem server entry is malformed. Repair or delete.
- `/home/fritz/42/Datasets/sabbath/mp3_throwaway` is 9-of-15 unusable: 8
  zero-byte files plus `Iron_Man.mp3` truncated to 43.2s (should be ~355s).
  Regenerate from the FLACs before any captioning run. Verified 2026-09-18.
- `dataset_manager.py` is ~5,850 lines (~253 KB). NEVER read it whole — read a
  line range by symbol. It is ~31% of the Python in this repo.

## [2026-09-20 — ACE-Step Kaggle rework]

- THE TEMPFILE + RANDOM-SLUG DESIGN IS WHY SONGS COULD NOT BE ADDED OR REMOVED.
  `RemoteCaptionWorker._run_real_kaggle` staged the audio into a
  `tempfile.mkdtemp()` that died with the run and uploaded it with
  `upload_audio_dataset` → `dataset_create_new` + a random 6-char slug, so every
  run made a NEW dataset and the previous one was orphaned. Now: a persistent
  local staging folder (`caption_staging_dir`, default
  `~/acestep_kaggle_staging`) whose CONTENTS are the dataset, a remembered slug
  (`caption_audio_dataset`), and `upload_or_update_audio_dataset()`
  (`modules/kaggle.py`) which pushes `dataset_create_version` when a slug is
  known. `modules/caption_kaggle_run.py` owns all of it, Qt-free and tested.
- THE RANDOM-SLUG DATASETS FROM EARLIER RUNS ARE STILL ON KAGGLE and count
  against the account's dataset quota. Nothing in the repo deletes them; clean
  them up on kaggle.com.
- THE KAGGLE PATH NOW STAGES THE ORIGINAL AUDIO, NOT THE 16 kHz PREVIEW. The
  worker's shared staging step (`run()`) still makes 16 kHz mono previews for the
  cloud backends, but the captioner HEARS the file it is given, so the Kaggle path
  re-derives the original `audio_path`, converts it to MP3 (`caption_convert_mp3`,
  `caption_mp3_bitrate`) and falls back to a full-quality COPY when ffmpeg is
  missing or refuses a file. A track is never silently dropped — that is the
  failure (`staging empty` → a run that captions nothing) the module reports.
- RESULTS NOW MATCH BY STAGED NAME, not by `basename.split("_preview")[0]` string
  surgery, so a rename on either side is reported as UNMATCHED instead of being
  silently mismapped onto the wrong song. `caption_kaggle_run.diff_captions()`
  holds the rule and `tests/test_caption_kaggle_run.py` pins it.
- THE 100-CHARACTER LOG PREVIEW IS GONE from `kernels/caption_kernel.py`
  ("it's set to truncate output to 100 lines. maybe audio too"): the full caption
  is printed per track. The ONLY remaining truncation is `MAX_AUDIO_DURATION`
  (seconds of audio fed to the model, 0 = whole file, default 120) and it is a
  GPU-memory limit, deliberately left at 120 — two T4s with
  `device_map="balanced"` are the constraint, not the reporting.
- NEW: batch diff review. `caption_batch_review` (default ON) holds every
  caption as a proposal in `caption_ai_raw` and shows ONE table at the end
  (existing vs new, per-track Keep/Use new/Edit…/Skip) instead of a modal per
  track. `apply_decision()` refuses blank and `ERROR:` proposals, so a bad run
  cannot erase an approved caption, and a replaced caption is preserved in
  `caption_before_kaggle`. Off = the old per-track dialog.
- 🅰 ACE-Step (Kaggle) IS ITS OWN PAGE now (new `ui/ace_step_tab.py`), reached
  through the grouped 🎤 Caption tab: `_build_grouped_tab` + an inner strip of
  `🅰 ACE-Step (Kaggle)` / `🎤 Other backends & MOSS`. The RUN buttons and the
  limit spins MOVED there, and `init_caption_tab` no longer creates them — the
  attribute names (`max_tokens_spin`, `caption_selected_btn`, ...) are unchanged
  so `save_pipeline_defaults` and every handler still resolve by name.
  `tests/test_ace_step_tab.py` pins both halves of that by-name contract.
- MOSS IS KEPT, MODULAR, AND STILL UNPROVEN. It never worked right in the user's
  hands and nothing here tries again: `kernels/moss_caption_kernel.py`,
  `workers/kaggle_moss.py`, `modules/moss_import.py` and its three tests are
  untouched and still reachable from the second Caption sub-tab, so a user who
  wants it keeps it. `modules/moss_import.py` still stores MOSS's raw style text
  as the caption (unchanged from the earlier note above).
- STILL OPEN, PRE-EXISTING: `workers/caption.py` keeps its F401s (`time`,
  `pathlib.Path`, `DeepSeekMusicOrchestrator`); the `shutil` F401 this rework
  introduced was removed again. `dataset_manager.py` F811/F841s are pre-existing
  and untouched.


## [2026-09-20 — caption credentials & the silent-fallback trap]

- THE ROOT CAUSE OF "ACE-Step wrote fake captions": `start_ai_captioning` called
  `resolve_backend()` bare, and that function DEGRADES SILENTLY — `ace_step` with
  no Kaggle key becomes DeepSeek (text-only, NO audio) and then "Local Rule
  Engine", whose `_run_local_dsp` writes canned template text ("balanced musical
  arrangement, organic dynamic response, defined lead instruments"). A page named
  ACE-Step (Kaggle) could therefore fill `caption` with plausible-looking
  placeholders. FIXED by `_resolve_caption_backend()`, which asks instead.
- THE DECISION (user, 2026-09-20): prompt for credentials and remember the answer
  PER BACKEND (`caption_cred_prompt_seen`); if declined, RUN a fallback but let the
  user CHOOSE it from a list of every engine (`caption_fallback_backend`); stamp
  ONLY the engines that never heard the audio. Credentials always override the
  memory, so a key that appears later goes straight to Kaggle without asking.
- ONLY THE AUDIO-BLIND ENGINES ARE STAMPED: `PLACEHOLDER_CAPTION_BACKENDS` =
  ("Local Rule Engine", "DeepSeek Cloud"). Kaggle ACE-Step, Gemini (audio-native)
  and an audio-capable custom endpoint are NOT stamped. The stamp is metadata
  (`caption_is_placeholder` plus a PLACEHOLDER note in `caption_ai_model`), never
  text inside the caption — that would itself become training data. Switch it off
  with `caption_stamp_placeholders: false`.
- `~/.kaggle/access_token` IS NO LONGER SILENTLY WRITTEN OR OVERWRITTEN.
  Measured in the installed SDK: `kagglesdk.get_access_token_from_env()` checks
  `KAGGLE_API_TOKEN` FIRST and only then `~/.kaggle/access_token`, so the env var
  alone authenticates. The file is now created ONLY when `remember_kaggle_key` is
  true, and NEVER when it already exists — it is a SHARED path other Kaggle tools
  read, and this app cannot tell its own stale copy from another tool's live token.
- THE THREE DUPLICATED INLINE KAGGLE PROMPTS (spatial / structural / advanced
  pipeline handlers) now call ONE `_ensure_kaggle_credentials()` — a dialog rather
  than two chained QInputDialogs, so "remember on this device" is chosen WHERE THE
  KEY IS ENTERED. `tests/test_kaggle_credentials.py` counts the call sites (== 3)
  so a fourth copy cannot appear unnoticed.
- STILL TRUE from the older notes: `modules/secrets_manager.py` falls back to a
  Fernet file keyed off `/etc/machine-id` when `keyring` is missing. Keyring IS
  available here, so that is the primary path — but the fallback is obfuscation,
  not protection, and must not be described as more than that.
- THE THREE PIPELINE PROMPTS STILL ASK ON EVERY RUN (no once-per-backend memory):
  those pipelines cannot run at all without Kaggle, so there is no fallback to
  remember and aborting is the only correct answer.

- THE WEIGHTS DATASET IS NOW EDITABLE IN THE UI (`kaggle_model_dataset`).
  `workers/caption.py`, `spatial.py` and `structural.py` all read it but NO widget
  existed anywhere — the only way to change it was hand-editing `settings.json`.
  New field: 🎤 Caption → 🅰 ACE-Step (Kaggle) → *Kaggle Run* → **Model weights
  dataset**, saved through `save_pipeline_defaults`.
- A BLANK WEIGHTS FIELD EXPOSED A METADATA BUG, NOW FIXED: all three pushers built
  `"dataset_sources": [audio_slug, model_slug]` unconditionally, so clearing the
  setting would have put an EMPTY STRING in the pushed kernel metadata (the API
  rejects it). They now call `modules/kaggle.py:dataset_sources(...)`, which drops
  blanks — an empty slug is how "download from Hugging Face instead" is expressed.
  `tests/test_kaggle_dataset_wait.py` pins both the helper and the fact that every
  caption-kernel pusher routes through it (that test caught `spatial.py` and
  `structural.py` still building the list by hand after the import was added).
- MOSS IS DELIBERATELY NOT ROUTED THROUGH `dataset_sources()`: `workers/kaggle_moss.py`
  builds its own list conditionally (`sources = [audio_slug]`, appending cached
  weights only when set), so it can never contain an empty entry.

- THE ACE-STEP PAGE NOW OWNS ITS TRACK SELECTION (2026-09-20, user report: "it
  tells me to pick them in dataset studio. there is not a place in dataset studio
  to add them" + "everything should be able to done with captions in the captions
  tab"). Three of that page's actions were bound to the Dataset Studio table's ROW
  selection — `staging_add_selected` (my own error: I reused `_selected_samples()`
  instead of providing a picker), `caption_selected_track` and
  `open_caption_editor`. Row selection is not a track picker and is not "a place
  to add tracks", so the instruction sent the user to a dead end. FIXED with
  `manager.ace_track_picker` (the existing `TrackPickerButton` the MOSS row uses:
  a menu with Select all / Select none / Select tracks missing captions plus a
  checkbox per track), and `_ticked_samples()` is now the page's ONLY selection
  source. Renamed: `staging_add_selected` → `staging_add_ticked`; the caption
  button reads "Caption Ticked". `refresh_ace_track_picker()` is called from
  `refresh_table()`, so the list and the ticks stay in step with the dataset.
- THE PAGE SAYS SO WHEN NOTHING IS TICKED (`ace_tick_status`, refreshed by
  `update_ace_tick_status()`): "Nothing ticked — use the “Tracks ▾” dropdown
  above", or "N of M ticked". No modal sending the user to another tab.
- THE REMAINING CROSS-TAB REFERENCE IS DELIBERATE AND DIFFERENT: adding SONGS to
  the dataset is still 🎛 Dataset Studio's job (Add single song / audio folder),
  which the empty-dataset hint names explicitly. Adding tracks to the *Kaggle
  upload* is the picker on the Caption page. The two "adds" are different actions
  and are now worded as such.

## [2026-09-24 -- the rework is committed]

Everything described above was sitting as a DIRTY TREE when the 2026-09-21
session froze mid-commit (five doc removals staged, the rest unstaged). It is
now in history: `c0bff6d` (docs), `124a29f` (connectivity + caption runs, 29
files). Nothing in this ledger should be read as "uncommitted work in progress"
any more; the items still marked unresolved are unresolved IN CODE.

## [2026-09-24 -- staging integrity and the caption strip]

TWO COMMITS: `5384a31` (staging + upload) and `991a321` (the page).

- THE STAGING FOLDER WAS LYING, and the CLASS of bug matters more than the instance:
  `staged_files()` filtered unusable files out of the LIST while the upload sent the
  FOLDER. So 32 zero-byte .mp3 corpses were invisible in the UI and uploaded anyway,
  and the kernel captioned each one into an ERROR row. TWO DIFFERENT FILTERS OVER THE
  SAME DATA is the defect; the corpses were only the symptom.
- ROOT CAUSE WAS NON-ATOMIC STAGING: ffmpeg creates its output file the instant it
  starts, so a process killed mid-encode left a 0-byte file under the track's REAL
  name. Fixed with `<target>.part` + `os.replace()`, so an interruption leaves scratch
  nothing uploads. Same shape as `wait_dataset_ready`: a leftover that LOOKS finished.
- Uploads now come from `_upload_payload()`, which drops only PROVABLE junk -- and not
  by the captioner's format list, because `upload_audio_dataset()` is shared by SIX
  other workers and a caption-specific list would have silently dropped their audio.
- STILL ON DISK, DELIBERATELY: those 32 corpses in `~/acestep_kaggle_staging`. They
  cannot reach an upload any more and the page names them ("⚠ 33 unusable file(s)
  ignored"), so removing them is the user's one click on 🧹 Clean -- not something an
  agent silently deletes out of a home directory.
- THE PAGE IS NOW ONE STRIP (tick → stage → caption → review), with everything
  set-once collapsed into ⚙ Settings and only the next step enabled, the reason in its
  tooltip. `update_ace_actions()` is the single decider. Tests pin BOTH the strip's
  order and the gating rules, because "buttons wherever" is exactly what creeps back
  when the order is not asserted.
- LATENT BUG FOUND WHILE DOING IT, NOT FIXED: `self.active_worker` is assigned on
  every run (DSP, spatial, caption, ...) and CLEARED NOWHERE -- the only `= None` is in
  `__init__`. Anything treating it as "is something running NOW" is wrong after the
  first run; that is why the caption busy state lives in its own `_caption_busy` flag.
- SUITE COUNT: 522 tests (508 before this work), all green offscreen; the boot check
  prints the strip order, the gating state and the staging counts, so a regression in
  any of the three is visible in one screen of output.

## [2026-09-24, later -- three self-disagreements fixed]

COMMITS `684155e` (kernel discovery) and `6744403` (staging + dataset identity).

- THE LESSON WORTH KEEPING from the empty run: a component that cannot do its job
  must FAIL, not return a valid empty result. `{"results": []}` was
  indistinguishable from "the model produced nothing", and that ambiguity cost
  days. Wherever a result can be empty, the empty case needs its own loud path.
- THE DATASET SLUG WAS NEVER SAVED. settings.json kept `caption_audio_dataset=""`
  while runs created `ace-audio-80d01a`, then `ace-audio-d66edb`: the slug lived in
  the worker's config dict and nothing persisted it, so EVERY run created a NEW
  dataset, the account collected orphans, and "re-runs push a new VERSION of the
  same dataset" never engaged. The worker now emits `dataset_slug_ready` and the
  window persists and displays it (a QThread does not write config files).
- "TICKED" MEANT TWO THINGS. ➕ Stage used the “Tracks ▾” ticks; ➖ Remove used the
  staging list's ROW selection — so ticking a song and pressing Remove answered
  "Nothing Ticked" about a song that was plainly ticked. The two views also show
  DIFFERENT NAMES for one song (dataset `aint_no_fun.flac` vs staged
  `aint_no_fun.mp3`), so nothing on screen connected them. Remove now uses the same
  ticks (mapped through `staged_name`), the list shows the mapping with
  "(not in this dataset)" for leftovers, and the staged name travels as item DATA
  instead of being parsed back out of a decorated label.
- THE KERNEL FALLBACK HAS THE SAME CAVEAT AS THE NOTEBOOK'S: with more than one
  audio-bearing dataset attached, walking `/kaggle/input` can resolve to the wrong
  one. It only runs when AUDIO_FOLDER yields nothing, and the resolved folder is
  echoed before the count — read that line before trusting a surprising result.

- THE APP STARTS EMPTY AND STAYS EMPTY. It does not reopen the last dataset
  (`load_dataset` is reachable only from 📂 Open), so after ANY restart "the list of
  all songs is gone / i can't select a single song" means "load the file again".
  THIS USER's live dataset is
  `/home/fritz/42/Datasets/acdc/_Backup/acdccapdone.json` (34 samples, 33 captioned,
  last written 2026-09-21); `_Backup/latest.json` and `originals_backup/acdc.json`
  are older 36-sample copies. The empty-state hint now names 📂 Open as well as
  🎛 Dataset Studio. Auto-reopening the last dataset was considered and REJECTED by
  the user as unnecessary — in normal use it is loaded once and kept open; only the
  frequent temporary restarts of this fix work made it look otherwise.
- THE PICKER WAS NEVER BROKEN. Verified offscreen with samples injected: Tracks ▾
  populates, ticks, and enables ➕ Stage the moment a dataset is present
  (`PICKER_OK`; menu = Select all / Select none / Select tracks missing captions /
  one action per track). So an empty dropdown means an empty DATASET, not a bug.

## [2026-09-24, later still -- the empty caption was THREE bugs in a row]

COMMITS `684155e` (mount fallback), `f1da69a` (the empty-state hint), `f06bc9e`
(Paths + whole songs). The run that kept returning no captions had all three:

1. THE MOUNT. The kernel hard-coded `/kaggle/input/<slug>` while this account's
   inputs mount under `/kaggle/input/datasets/<owner>/<slug>/` — proven by the
   kernel's OWN log, where the weights resolved to
   `.../datasets/michelmoalem9b/...` in the same session that the audio folder held
   0 files. The walk + `/kaggle/input` fallback + a hard failure are now ported from
   the MOSS kernel. `was empty; found audio elsewhere` in a MOSS log was its
   fallback WORKING; reading it as evidence of a missing attachment cost days.
2. THE TYPE. That port returned STRINGS; THIS kernel's callers use the Path API
   (`batch[0].name`, `f.name` as the results key). The model loaded, the audio was
   found, and the run died with `AttributeError: 'str' object has no attribute
   'name'` four minutes in. LESSON: copy the function AND its caller's contract.
   The kernel's tests grepped SOURCE TEXT, which cannot see a type mismatch, so
   `tests/test_caption_kernel.py` now EXECUTES the pure functions — see
   `_kernel_function()`, which compiles one function out of the template.
3. THE COVERAGE. One pass over the first 120 s of every track — "less than half" of
   a four-minute song. `caption_whole_song` (DEFAULT ON) tiles the whole file in
   passes and merges them into one caption with a text-only pass; if that merge
   fails or comes back empty the first pass is used, so a track is never lost.
   "Pass length (sec)" is the CHUNK size (GPU memory), not the coverage.

- THE WAIT IS NOW BUDGETED (tracks x passes, capped at 4 h) instead of a flat
  20 minutes, which whole-song captioning exceeds — and a timeout is
  indistinguishable from a failure.
- `workers/spatial.py` and `workers/structural.py` pass `WHOLE_SONG=False`
  DELIBERATELY: they slice the audio into sections themselves, so kernel-side
  windowing would cut every section twice. `tests/test_caption_spec.py`'s
  placeholder contract is what caught that omission.
- STILL OPEN — A FAILED RUN WRITES NO LOG TO DISK. `fetch_kernel_logs()` embeds the
  log in the RuntimeError, so it reaches the error dialog and nothing else; a `.log`
  file appears in the output folder only when the kernel SUCCEEDS and its output is
  downloaded. "An error" was answerable this time only because the log was fetched
  from Kaggle's API by hand. Fix that next.
- THE WINDOWS ARE FIXED OFFSETS (0, 120, 240...). Cutting at verse/chorus
  boundaries instead is the known upgrade — `kernels/structure_kernel.py` already
  produces SongFormer sections and `MIN_SEGMENT_SEC` exists. The user deferred it.

- THE FOURTH BUG, AND THE ONE THAT GOT FURTHEST (commit `aa399d4`):
  `NameError: name 'MAX_AUDIO_DURATION' is not defined` at the first track of run
  ace-caption-cadcf7 — after the mount fallback had found the audio and the count
  line reported it honestly. `{{MAX_AUDIO_DURATION}}` is substituted as a LITERAL, so
  it had never existed as a variable, only as `max_seconds=120` and `str(120)`. It is
  now bound once, by name, and used from there.
  THE GUARD MATTERS MORE THAN THE FIX: `tests/test_caption_kernel.py` substitutes
  every placeholder into the real kernel (`fill_kernel`) and asserts that no name is
  READ without being bound somewhere — a deliberately conservative F821 that cannot
  fire on valid code, plus a second test proving it FIRES on the exact snippet that
  failed. THREE runtime-only defects in a row (mount path, str-vs-Path, undefined
  name) were invisible to source greps; two are now covered by EXEC tests and this one
  by a static check. THE GENERAL LESSON: for a file that can only RUN remotely, assert
  against the FILLED code, not against its text.

- THE FIFTH BUG, AND THE FIRST RUN THAT COMPLETED (commit `5f45592`): run
  ace-caption-b34710 finished with `DONE 1`, and its one result row was
  `ERROR: CUDA out of memory. Tried to allocate 32.61 GiB.` — the whole-song path had
  sent the track's TWO 120 s windows through the model in ONE audio forward pass. The
  audio tower's attention is quadratic in the audio tokens, so two clips is far more
  than twice one, on a card that already held 9.7 GiB of 14.56 GiB. `_pass_batches()`
  now yields ONE CLIP PER PASS and a test EXECUTES it, so the constraint cannot be
  "optimised" away again; the finished pass's cache is released before the next.
  PROVEN BY THAT SAME RUN, and worth keeping: the per-track except marked the track
  instead of losing it, `apply_decision()` refused the ERROR proposal so nothing
  reached the dataset, and the mount fallback plus its honest `(searched /kaggle/input)`
  line both behaved as designed.
- PATTERN WORTH REMEMBERING: all five defects (mount path, str-vs-Path, undefined name,
  batched audio, plus the earlier 0-byte staging) were INVISIBLE to the local suite and
  each cost a full Kaggle run (~4-6 min plus GPU) to discover. The guards now matter
  more than any single fix: EXEC tests for pure kernel functions, a static
  unbound-name check on the FILLED kernel, and an executed "one clip per pass" rule.
  When adding kernel behaviour, add the assertion that would have caught it running on
  Kaggle, not one that merely finds its text.

## [HARNESS] -- this machine's VS Code terminal (recorded 2026-09-24)

Not a repo bug, but it masquerades as one, and it is what "the agent froze"
meant here. The terminal's shell-integration OUTPUT CAPTURE is broken: commands
run, their output shows up in the raw terminal dump, and the tool still reports
"completion could not be observed" and exits 1. What that does to long commands:

- A NON-DETACHED command is killed when the next command arrives ("starting
  another foreground command will attempt to close it"). A job backgrounded
  with a plain `&` dies with it, and a redirected python buffers its stdout, so
  the kill leaves an EMPTY LOG. An empty log therefore means "killed", not
  "failed" -- the tail of the 2026-09-21 session and the first suite run here
  both ended exactly that way.
- MEASURED: a full-suite run reached `[100%]` and then vanished with no summary
  line (`ps -p <pid>` afterwards printed only a header). The same suite launched
  with setsid wrote `EXIT=0` and finished normally.
- `DatasetManager()` is not hung but is slow ONCE per process: ~30-60 s, nearly
  all of it Qt's FFmpeg multimedia plugin init. A boot check read at 45 s looks
  hung and is not -- wait for the first line before concluding anything.

THE RECIPE that works here:

    setsid nohup bash -c 'CMD > /tmp/out.log 2>&1; echo EXIT=$? >> /tmp/out.log' \
      < /dev/null > /dev/null 2>&1 & disown

then poll `/tmp/out.log` with a FILE READ rather than with another command, and
use `python -u` for anything that prints. setsid moves the job into its own
session, so it survives the terminal being closed under it.

- `pytest.ini` already passes `addopts = -q`, so adding `-q` again makes it
  `-qq`, which SUPPRESSES the trailing "N passed" line -- a green run then looks
  like bare dots with no summary. `EXIT=0` is the real signal; for a count use
  `--collect-only -q -o addopts=`.
- VERIFIED THIS WAY on 124a29f: 508 tests collected, 508 passed (`EXIT=0`;
  45.65 s to collect, ~90 s to run), and an offscreen boot check printed
  BOOT_OK with all 12 ACE-Step page attributes present, `tab_pages: 7`,
  `grouped_tabs: 3`, `caption_backend: ace_step`.
- Unattributed, recorded so it is not re-investigated as drift: `.git/index`
  carried a 2026-09-24 03:01:53 mtime before this session changed anything
  (most likely the VS Code git extension). `git status` agreed with the index
  file, so nothing was corrupt.

## [BASELINE SWEEP]

RUN 2026-09-15 — first pass complete. Tools: vulture 2.16, flake8 7.3.0
(installed into `.venv` for this scan; NOT app runtime deps).

```bash
.venv/bin/python -m vulture . --exclude .venv,kernels,__pycache__,scripts,.git --min-confidence 80
.venv/bin/python -m flake8 --select=F401 . --exclude=.venv,kernels,__pycache__,scripts,.git
```

- RESULT: 3906 net lines of dead code removed; app boots clean (offscreen test).
- OUTSTANDING: 41 unused imports in peripheral files
  (`workers/advanced.py`(6), `workers/structural.py`(5), `stem_separator.py`(5),
  `workers/health.py`(4), `workers/caption.py`(3), ...). Do NOT blindly strip —
  several of those modules re-import names lazily inside functions. Verify each
  before removing.

This section is a one-time audit, not a per-commit chore: `git diff` cannot see
dead code that was committed long ago and never edited, which is why it needs
its own pass. Re-run only on request, and put the flagged lines here.
